

/*
   When prioritization of types matter.
   For example if there's no placeType or if there's a city and a county and the
   placeType is merge then type priorities is the determinate
 */

const ahjTypePriorities = [];

/*
   Purpose is to flatten the ahjRefs collection and to map its output to an object by id.
 */

const refsById = ahjRefs => {

};

//getDesignedAhj
module.exports = (function( ){
   const {
      place, // placeType determined by census api
      ahjRefs // array of sources(source{ id, name, type, inherit})
   } = this.ahj;

});
